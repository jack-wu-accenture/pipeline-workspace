.DS_Store

# Opportunity raw inputs — not tracked in git (manage separately via cloud storage)
opportunities/*/input/

# Claude local settings — machine-specific, not portable
.claude/

# OSS credentials — never commit
scripts/oss_config.json
